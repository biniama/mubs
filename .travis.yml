sudo: required
language: groovy
jdk:
    - openjdk7
branches:
    only:
      - master
before_script:
    - chmod +x grailsw
 script: ./grailsw refresh-dependencies
        && ./grailsw test-app
      